{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@fastify/error": "^3.3.0",
    "@prisma/client": "^5.0.0",
    "cpf-cnpj-validator": "^1.0.3",
    "dotenv": "^16.3.1",
    "fastify": "^4.19.2",
    "fastify-type-provider-zod": "^1.1.9",
    "prisma": "^5.0.0",
    "zod": "^3.21.4"
  },
  "devDependencies": {},
  "scripts": {
    "prebuild": "yarn rimraf build",
    "build": "yarn tsc -p tsconfig.build.json",
    "postbuild": "yarn tsc-alias -p tsconfig.build.json",
    "dev": "yarn tsx watch --clear-screen=false -r dotenv/config index.ts"
  }
}
